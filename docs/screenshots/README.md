# Screenshots

These are **representative SVG renders** of the dashboard (dark theme, real layout and
sample data) used in the top-level `README.md`. They illustrate the UI without pulling
in any real hostnames, IPs, or tokens.

To use your own captures instead, drop PNGs here and update the image paths in
`../README.md`:

- `logs.png` — Logs explorer (source dropdowns, traffic chart, top paths/IPs, stream)
- `system-map.png` — Resources → System map (enforcement targets and their state)
- `overview.png` — Overview (traffic/attack summary, WAF offenders, anomalies)

Sample data only — no real infrastructure details.
