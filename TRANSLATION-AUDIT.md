# Translation audit (read-only)

Model: source is Russian; EN is the live translation (RU2EN) + I18N keys; RU mode shows raw source.

Leak = Russian visible in EN, or English visible in RU.


## 0. Russian-phrase keys in I18N but consumed via _trText (HIGH — these translations silently don't apply)

_Fix: move to RU2EN, or call t() instead of _trText. If not in RU2EN → leaks Russian in EN._

- L968 `Удалить это подключение?` → `Delete this connection?` — NOT in RU2EN → leaks
- L968 `активно` → `active` (already in RU2EN ✓)
- L967 `доступен, токен принят` → `reachable, token accepted` — NOT in RU2EN → leaks
- L962 `нет ingress-таргетов` → `no ingress targets` — NOT in RU2EN → leaks
- L972 `нет данных о хосте` → `no host data` — NOT in RU2EN → leaks
- L967 `нет связи` → `no connection` — NOT in RU2EN → leaks
- L972 `нет токена` → `no token` — NOT in RU2EN → leaks
- L966 `ошибка` → `error` (already in RU2EN ✓)
- L968 `пока нет — проверь соединение и нажми Save` → `none yet — test the connection and click Save` — NOT in RU2EN → leaks
- L967 `проверяю` → `checking` — NOT in RU2EN → leaks
- L968 `сделать активным` → `make active` — NOT in RU2EN → leaks
- L968 `сохранено и активно` → `saved and active` — NOT in RU2EN → leaks
- L966 `сохраняю…` → `saving…` (already in RU2EN ✓)
- L972 `таргет` → `target` — NOT in RU2EN → leaks
- L972 `токен задан` → `token set` — NOT in RU2EN → leaks
- L967 `токен не принят` → `token rejected` — NOT in RU2EN → leaks
- L967 `укажи hostname выше` → `enter a hostname above` — NOT in RU2EN → leaks

## 1. I18N en-value contains Russian (leaks Russian into EN mode)


## 2. I18N key present in one dict, missing in the other (dotted/latin keys only)


## 3. I18N ru-value looks English (leaks English into RU mode)

- L989 key `ing.h` → ru=`Ingress`
- L1005 key `logs.access` → ru=`access`
- L1005 key `logs.error` → ru=`error`

## 4. RU2EN value still contains Russian (EN mode stays Russian)


## 5. Russian literals in source NOT fully covered by RU2EN (EN-mode leak)

_Deduped by normalized text; `${...}` and tags stripped. Each shown once at first line._

- L161 `Изменить название и иконку`
- L253 `Спросить словами: «404 на /admin за 30 минут»`
- L257 `хост (example.com)`
- L261 `за какое время`
- L279 `IP или CIDR (напр. 203.0.113.0/24)`
- L289 `перечитать денлист в ingress (после kubectl apply)`
- L291 `поиск по IP/причине…`
- L302 `окно анализа`
- L320 `форс-перепроверка ИИ (игнор кэша)`
- L331 `окно`
- L452 `выгрузить текущую форму как JSON`
- L459 `напр. WP-login brute`
- L513 `кого бы забанило (dry-run, без бана)`
- L536 `IP (напр. 203.0.113.45)`
- L592 `Куда применять: к какой группе таргетов (нод/ingress). Пусто = все.`
- L594 `добавить, даже если апка считает паттерн слишком широким`
- L602 `Вместо создания нового правила дописать паттерн в существующее (одно правило = пачка путей)`
- L900 `монитор трафика и атак`
- L921 `спокойно`
- L923 `тревога`
- L947 `Отмена`
- L1208 `скрытые карточки:`
- L1239 `— не определено`
- L1304 `⚠ атака`
- L1327 `карта не загрузилась (CDN недоступен)`
- L1338 `ошибка карты:`
- L1386 `✓ доверенный:`
- L1393 `☁ Это адрес Cloudflare , а не реального посетителя. Бан бесполезен — реальный клиент скрыт за фларой`
- L1398 `4xx кроме 403 — 404/422/429 и т.п. (не блок, ошибки запроса)`
- L1402 `сеть:`
- L1410 `⛔ бан бесполезен (Cloudflare)`
- L1410 `⛔ заблокировать на ingress`
- L1425 `на ingress? Будет возвращаться 403 для этого источника.`
- L1427 `(активно правил:`
- L1428 `Не удалось:`
- L1442 `IP или CIDR для бана:`
- L1457 `только автобан`
- L1465 `blocklist-api не настроен (BLOCKLIST_API_URL пуст) — кнопки блокировки отключены.`
- L1468 `ошибка загрузки денлиста`
- L1588 `✓ просмотрено`
- L1588 `пометить`
- L1589 `снять отметку «просмотрено»`
- L1589 `пометить просмотренным`
- L1597 `сканер`
- L1597 `юзер`
- L1597 `подозрит.`
- L1605 `профиль / вся инфа`
- L1607 `хитов 4xx`
- L1620 `подозрительно:`
- L1621 `подозрительная активность`
- L1639 `считаю…`
- L1647 `IP · хитов${cf.length?`
- L1650 `чисто — реальных сканеров нет за окно ( )`
- L1652 `за Cloudflare (реальный IP скрыт)`
- L1656 `ошибка загрузки`
- L1664 `Снять ВСЕ блокировки с ingress? (статический deny-список приложения не затрагивается)`
- L1671 `IP на ingress? Каждый будет получать 403.`
- L1674 `, пропущено:`
- L1674 `Активных правил:`
- L1696 `IP · ошибок 4xx${cf.length?`
- L1699 `чисто — аномальных 4xx нет за окно ( )`
- L1712 `Все уже оценены — жми «Переоценить» для форс-перепроверки.`
- L1728 `подозрительных IP на ingress?`
- L1748 `проверяю … (несколько секунд)`
- L1754 `Полный отчёт: проверки SSL, заголовков, безопасности, DNS, портов, стека.`
- L1763 `Атака`
- L1770 `Ошибка:`
- L1773 `⤓ экспорт всех IP`
- L1775 `ошибка запроса`
- L1793 `отдано: B`
- L1824 `забанить IP из результата на ingress (403)`
- L1836 `поиск…`
- L1850 `все коды ( )`
- L1887 `уже забанен на ingress (правило`
- L1888 `из «показать запросы»`
- L1890 `IP уже забанены на ingress.`
- L1891 `новых IP на ingress?`
- L1891 `уже забанены — пропущу)`
- L1891 `Каждый будет получать 403.`
- L1894 `, пропущено (доверенные/приватные):`
- L1894 `, уже было:`
- L1911 `Сначала «Найти» — нет результатов для экспорта`
- L1916 `🤖 разбираю запрос… (LLM)`
- L1920 `LLM понял так:`
- L1930 `404 (перебор путей)`
- L1930 `5xx (сервер)`
- L1930 `CRS-пейлоады (SQLi/XSS)`
- L1935 `: — было ~ , рост ×`
- L1936 `: (раньше почти не было)`
- L1944 `⚠️ Часть трафика доходит до приложения — стоит разобраться (детали ниже).`
- L1945 `✅ Атака отбита: всплеск — это заблокированный трафик (deny/403), до сайта он не дошёл. Срочных дейст`
- L1949 `проходит`
- L1949 `блокируется`
- L1968 `Лента AI-разборов — клик, чтобы развернуть «почему»`
- L2080 `(топы за — больше окно упирается в лимит Loki)`
- L2091 `⚠ Дашборд не получает данные от backend ( ).`
- L2109 `⚠ Замечено ${dg.attacks.map(a=>`
- L2110 `✅ Рекомендации ${dg.recommendations.map(a=>`
- L2120 `ошибка генерации`
- L2128 `возобновить`
- L2128 `пауза авто-обновления`
- L2133 `свернуть/развернуть`
- L2170 `удалено правило 403`
- L2171 `переключатель 403`
- L2184 `— нет данных о применении`
- L2262 `укажи hostname выше`
- L2263 `проверяю`
- L2267 `доступен, токен принят`
- L2268 `нет связи`
- L2268 `токен не принят`
- L2279 `сохранено и активно`
- L2308 `Удалить это подключение?`
- L2363 `пока нет CF-таргетов. Нажми «+ CF target».`
- L2384 `(задан — введи для замены)`
- L2384 `(обязательно)`
- L2409 `ID CF-таргета (напр. nginx-aaa-cloudflare):`
- L2422 `не сохранилось:`
- L2425 `Удалить CF-таргет «`
- L2425 `»? Он отвяжется от групп/правил. Список/правило в самом Cloudflare не удаляются автоматически.`
- L2435 `нет ответа.`
- L2493 `сначала задай id канала и сохрани`
- L2495 `✓ отправлено`
- L2544 `только через ENV (STORE=configmap — ConfigMap хранит открытым текстом)`
- L2580 `нет Cloudflare-таргетов в targets.`
- L2608 `ошибка загрузки:`
- L2615 `blocklist-api не настроен (BLOCKLIST_API_URL пуст) — ноды недоступны.`
- L2667 `# ENROLL_SECRET не задан в blocklist-api — задай его, чтобы агенты могли регистрироваться.`
- L2668 `⚠ Задай PUBLIC_URL в blocklist-api (адрес, по которому VM достучатся до API) — без него команда непо`
- L2677 `»? Агент перестанет получать баны, пока не перерегистрируется.`
- L2697 `уже есть`
- L2703 `Добавлено частично:`
- L2703 `Отметь основные правила звёздочкой ★ во вкладках 403 и Автобан.`
- L2724 `не удалось загрузить:`
- L2888 `🛡️ защищённые (в 403 добавить нельзя, общий список с автобаном):`
- L2905 `: 403 выключен — включить`
- L2906 `: нет связи с бэкендом`
- L2907 `дрейф`
- L2907 `: дрейф рендера — нажми ↻ ресинк в Денлисте`
- L2908 `: 403 активен — выключить`
- L2928 `· найдено`
- L2931 `ничего не найдено по «`
- L2937 `симв.`
- L2944 `(без имени)`
- L3018 `добавить путь (как «Путь содержит», напр. /.env)`
- L3031 `» уже есть — пропущено`
- L3039 `в правиле не осталось ни одного пути — тогда удали правило целиком.`
- L3058 `Точный URI`
- L3059 `Wildcard (* = что угодно)`
- L3059 `/admin/* или *.sql`
- L3059 `Regex (как nginx ~*)`
- L3071 `✗ некорректный regex:`
- L3073 `✗ кавычка " запрещена — сломает nginx`
- L3074 `⚠ поймает обычный путь «`
- L3074 `» — отклонится (или включи force)`
- L3078 `🚫 забанит`
- L3079 `✓ пропустит`
- L3103 `— (имя только для нового)`
- L3111 `не удалось сменить группу:`
- L3125 `правило не найдено`
- L3127 `• уже есть в «`
- L3131 `✓ в «`
- L3141 `• такое правило уже есть — не дублирую`
- L3142 `✓ создано`
- L3150 `Выключить ВСЮ секцию 403? Все правила по путям перестанут блокировать — сканеры снова будут проходит`
- L3155 `Добавить базовый набор правил сканеров одним правилом «base-scanners»? (повторный засев обновит его,`
- L3157 `✓ базовый набор добавлен/обновлён. Если у тебя был свой ручной if в ингрессе — убери его, чтобы не д`
- L3164 `не удалось получить правила автобана`
- L3170 `выбери правило автобана → станет правилом 403`
- L3172 `★ все ( ) — кроме семейств`
- L3176 `— семейство, нельзя`
- L3177 `· пут.`
- L3187 `✓ создано/обновлено:`
- L3188 `пропущено:`
- L3198 `» уже ловится правилом «`
- L3198 `». Всё равно добавить отдельным правилом?`
- L3199 `уже ловится`
- L3205 `не удалось добавить в 403:`
- L3237 `список пуст — автобан считает все пути`
- L3244 `не забудь «Сохранить»`
- L3304 `забанено:`
- L3317 `(но это приватный/уже-denied статический диапазон)`
- L3332 `и ещё`
- L3333 `сошлось по путям (24ч):`
- L3335 `примеры путей не найдены (активность старше 24ч)`
- L3339 `⌛ истёк`
- L3345 `автобан · · ≥N/окно`
- L3357 `ошибка загрузки логов:`
- L3363 `событий автобана (последние сверху)`
- L3363 `событий автобана пока нет`
- L3364 `Автобан ещё никого не банил (или kill-switch был выключен).`
- L3372 `пути не сохранены`
- L3374 `снят`
- L3380 `сошлось по путям`
- L3430 `➕ в основные (403 + автобан)`
- L3432 `🚫 добавить в правило 403:`
- L3435 `⛔ в автобан (бан IP по порогу):`
- L3437 `нет автобан-правил с путями. создать правило →`
- L3447 `нет правила`
- L3450 `✓ уже есть`
- L3453 `✓ добавлено`
- L3562 `не JSON:`
- L3567 `массив: загрузил первое, для пачки жми «Сохранить из JSON»`
- L3568 `✓ загружено в форму`
- L3704 `такая группа уже есть`
- L3796 `· прогон назад: включено правил , новых забанено`
- L3797 `, уже в бане`
- L3798 `, отложено по лимиту`
- L3803 `🟢 ВКЛЮЧЁН — включённые правила банят на ingress каждые с (≤ новых/проход).`
- L3815 `Включить автобан? Включённые правила начнут РЕАЛЬНО банить IP на ingress каждые с (до новых за прохо`
- L3882 `· уже в денлисте (пропущено)`
- L3883 `0 новых IP под условие (≥ за ). Никого нового бы не забанило.`
- L3884 `Забанило бы новых IP (≥ за ) :`
- L3896 `ИЛИ`
- L3905 `(по 1 пути)`
- L3905 `(∑ путей)`
- L3950 `лого`
- L3958 `файл больше 180KB — выбери поменьше`
- L3969 `ошибка сохранения`
- L3973 `не удалось сохранить`


=== COUNTS ===
regions: I18N (959, 1011) sep 987 RU2EN (1024, 1149)
en keys 125 ru keys 108 ru2en 458
