.venv/
__pycache__/
*.db
*.db-wal
*.db-shm
node_modules/
frontend/dist/
*.pyc
.DS_Store
**/.DS_Store

# secrets / local config — never commit real env files (the *.example are the templates)
.env
**/.env
*.env
!*.env.example
deploy/hosts.txt

# agent / runtime state
state.json
positions.yaml
/data/

# local Claude state (preview launch config + any machine-specific bits).
# NB: persistent memory lives OUTSIDE the repo (~/.claude/projects/…) and can't be
# committed anyway — this just guards anything local under .claude/ in-repo.
.claude/
